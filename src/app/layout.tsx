import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/sonner";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "DineFlow - Delicious Food Delivered Fast",
  description: "AI-powered restaurant experience with smart menu descriptions, seamless ordering, and table reservations. Premium dining, reimagined.",
  keywords: ["DineFlow", "AI Restaurant", "Food Ordering", "Smart Menu", "Premium Dining"],
  authors: [{ name: "DineFlow" }],
  icons: {
    icon: "/logo.svg",
    shortcut: "/favicon.ico",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster richColors position="top-right" />
      </body>
    </html>
  );
}
